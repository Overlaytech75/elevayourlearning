# Roadmap

## Run locally without Lovable Cloud
- [x] Client env flags for optional AI + Google OAuth (cloud defaults unchanged)
- [x] Env-configurable AI provider; app works with no AI key
- [x] Clean "AI unavailable in local development" states (mentor, AI tools, study)
- [x] `.env.example` + `.env.local` gitignored
- [x] `LOCAL_SETUP.md` with exact steps (local Supabase via Docker + existing migrations)
